Place the trained oil-spill segmentation model here.

Recommended first research model:
- U-Net or lightweight segmentation network
- Input: Sentinel-1 VV/VH
- Output: binary oil-spill mask

The current prototype intentionally uses a deterministic demo detector so the UI and end-to-end workflow can be tested immediately.
